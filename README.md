# Satellite Telemetry Server
Tornado-based WebSocket server that reads RSSI data from an RTL-SDR or simulates satellite housekeeping telemetry for Open MCT.

## Requirements
Install dependencies via pip:
```bash
pip install -r requirements.txt
